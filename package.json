{
	"name": "verdit-monorepo",
	"version": "1.0.0",
	"private": true,
	"scripts": {
	  "build:main": "cd main && npm install && npm run build",
	  "build:pt": "cd pt && npm install && npm run build",
	  "build:crypto": "cd crypto && npm install && npm run build",
	  "build:agentic": "cd agentic && npm install && npm run build",
	  "build:all": "npm run build:main && npm run build:pt && npm run build:crypto && npm run build:agentic"
	}
  }